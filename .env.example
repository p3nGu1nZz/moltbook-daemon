# Moltbook API Configuration
MOLTBOOK_API_KEY=your_moltbook_api_key_here

# Optional: Override API base (default is https://www.moltbook.com/api/v1)
# NOTE: Moltbook redirects on non-www hosts can strip Authorization headers.
MOLTBOOK_API_BASE=https://www.moltbook.com/api/v1

# Optional: Submolt to post updates into (used with --post)
MOLTBOOK_SUBMOLT=general

# Optional: Output limits for generated posts
MAX_CONTENT_CHARS=3500
MAX_COMMITS=10
MAX_FILES=25

# Optional: Override where the daemon stores its local state
# STATE_FILE=.moltbook_daemon_state.json

# Project Directory - Path to local repo for source material
PROJECT_DIR=/path/to/your/project
